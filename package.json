{
	"devDependencies": {
		"prisma": "^6.12.0",
		"nodemon": "^3.1.10",
		"ts-node": "^10.9.2",
		"dotenv": "^17.2.0"
	},

	"scripts": {
		"dev": "next dev",
		"build": "next build",
		"start": "nect start",
		"db:generate": "prisma generate",
		"db:migrate": "prisma migrate dev"
	}
}
